---
paths: [ '**/references/tables/*.md' ]
---
When writing/updating `tables/` documents: be very very terse, don't handhold, write only generalizable facts and trigger phrases; the audience is: other LLMs in other tenants. If you have to leave partial guidance out, do it.
